{{ form_start(form, {'action': path('user_group_edit', { 'id': user_group.id, 'redirect': redirect })}) }}
    {{ form_widget(form) }}
    <button class="btn btn-info submit-btn">Update</button>
{{ form_end(form) }}