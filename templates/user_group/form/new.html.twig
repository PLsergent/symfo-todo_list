{{ form_start(form, {'action': path('user_group_new', { 'id': project.id, 'redirect': redirect })}) }}
    {{ form_widget(form) }}
    <button class="btn btn-info submit-btn">Create Group</button>
{{ form_end(form) }}