{{ form_start(form, {'action': path('task_edit', { 'id': task.id, 'redirect': redirect })}) }}
    {{ form_widget(form) }}
    <button class="btn btn-info submit-task">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}