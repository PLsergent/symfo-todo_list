{{ form_start(form, {'action': path('todo_edit', {'id': todo.id })}) }}
    {{ form_widget(form) }}
    <button class="btn btn-info submit-todo">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}