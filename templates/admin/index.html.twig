{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Admin interface</h1>

    No exverything is working properly due to changes in the main application.

    {% for entity in entities %}
        <h4><a href="{{ path('admin_' ~ entity ~ '_index') }}">{{ entity|capitalize }}</a></h4>
    {% endfor %}
</div>
{% endblock %}
