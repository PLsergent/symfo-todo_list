{{ form_start(form, {'action': path('category_edit', { 'id': category.id, 'redirect': redirect })}) }}
    {{ form_widget(form) }}
    <button class="btn btn-info submit-btn">Update</button>
{{ form_end(form) }}